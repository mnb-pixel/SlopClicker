# Alte Tab-Pfade lagen an der Domain-Wurzel (siehe routes.js) - seit dem Landingpage-Umbau
# (main.jsx/LandingPage.jsx) ist "/" keine Spiel-Route mehr, das Spiel liegt komplett unter
# /play. 301 statt SPA-Fallback, damit bereits indexierte/verlinkte alte URLs sauber auf die
# neuen /play-Pfade umziehen, statt clientseitig auf Tab 1 zurückzufallen (siehe main.jsx/
# App.jsx-Catch-all, der bei einer nicht gematchten Route ohnehin greifen würde - dieser
# Redirect ist die sauberere Variante für bereits von Google gecrawlte URLs).
# /impressum bewusst NICHT hier drin: das ist keine Tab-Route, sondern ein von main.jsx
# unverändert an der Domain-Wurzel behandeltes Rechtstext-Overlay (siehe routes.js
# LEGAL_ROUTES) - das ist vom /play-Umzug gar nicht betroffen.
# Von Cloudflare Workers Static Assets ausgewertet (siehe wrangler.jsonc, assets.directory),
# genau wie _headers - dieselbe Syntax wie bei Cloudflare Pages.
/shop           /play/shop           301
/statistik      /play/statistik      301
/einstellungen  /play/einstellungen  301

# Spiel-Deep-Links (/play/shop usw.): statt des SPA-Fallbacks (dist/index.html = die
# vorgerenderte Startseite, siehe scripts/prerender.mjs) die leere Spiel-Hülle dist/play.html
# ausliefern (Status 200 = Rewrite, keine Weiterleitung - die URL bleibt erhalten, damit
# react-router in App.jsx den Tab aus dem Pfad lesen kann). Ziel bewusst "/play" statt
# "/play.html": Cloudflare normalisiert .html-Pfade (html_handling "auto-trailing-slash")
# per 307 auf die Variante ohne Endung - als Rewrite-Ziel würde /play.html dadurch zu einer
# echten Weiterleitung auf /play, und der Unterpfad (/shop) ginge verloren. /play selbst
# braucht keine Regel: play.html wird direkt unter /play ausgeliefert.
/play/*         /play                200

# Versteckte 3D-Testansicht (siehe src/main.jsx / VoxelApp.jsx): /voxel selbst wird direkt
# aus dist/voxel.html ausgeliefert (leere Hülle, noindex - siehe scripts/prerender.mjs),
# etwaige Unterpfade fallen per Rewrite auf dieselbe Hülle zurück, damit ein Reload dort
# nicht auf der vorgerenderten Startseite landet. 200 statt 301: die URL soll erhalten
# bleiben.
/voxel/*        /voxel               200
